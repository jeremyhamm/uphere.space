version: '3'
services:
  api:
    image: uphere-space-api
    build: .
    networks:
      - backend
    ports:
      - "3000:3000"

  web-cli:
    image: uphere-space-api
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge
